# MeetScribe Core Dependencies
# For optional dependencies, use: pip install meetscribe[all]
# Or specific features: pip install meetscribe[whisper,discord]

# Core dependencies (required for basic functionality)
pyyaml>=6.0,<7.0
python-dotenv>=1.0.0,<2.0.0
click>=8.1.0,<9.0.0
rich>=13.0.0,<14.0.0
tqdm>=4.65.0,<5.0.0
